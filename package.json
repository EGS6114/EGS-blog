{
  "name": "aiovtue-hugo-blog",
  "private": true,
  "scripts": {
    "dev": "node scripts/fetch-bangumi.mjs && hugo server -D --disableFastRender",
    "compress-mouse": "node scripts/compress-mouse-assets.mjs",
    "build": "node scripts/build.mjs"
  },
  "devDependencies": {
    "decode-ico": "^0.4.1",
    "sharp": "^0.34.5"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "sharp"
    ]
  }
}
